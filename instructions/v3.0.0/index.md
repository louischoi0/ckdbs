# instructions/v3.0.0 — the version's operator input

Opened 2026-09-02 on `worktree-v3.0.0-arch-revision` at `d15b5ac`
(`v2.7.0-134-gd15b5ac`), the first thing written under `instructions/`
since the tree was emptied at `7f0193b`.

**The version in one sentence:** v3.0.0 is the big-bang change — one WAL
stream for the instance, shared memory in place of shared-nothing,
ownership decomposed into affinity and allocation — and it is built
milestone by milestone from AR0's §8, each milestone under one work
order, the `cores = 1` path byte-identical throughout.

**The operator named the version on 2026-09-03** (D15, marked — AR0-M6):
it is **v3.0.0**, and `bench/v3.0.0/` is the version's series rather than a
proposal's. The **annotated tag is a separate act and has not been taken**,
so `git describe --tags` still reads `v2.7.0-*` and every measurement
carries that until one is cut. AR0-M6 says what a tag cut at this commit
would and would not be able to claim.

**Two governing drafts, not one.** AR0 is the big-bang change and its §8
is the milestone chain every work order here belongs to. **AR1, added
2026-09-03, is not part of that chain**: its subject is Cabin and the
function catalog, it depends on neither the pool nor the view, and its
own §11 states the single place the two cross. A work order cites the
draft it serves; neither draft's numbering reaches the other's.

| document | what it is |
|---|---|
| `ar0-architecture-revision.md` | The governing draft, verbatim as the operator supplied it on 2026-09-02, with AR0-V appended: the source-read verification at `d15b5ac`, which is what the tree says wherever the body disagrees, and **AR0-M appended**: the operator's marks of 2026-09-03 on D1 (b, conditionally), D2 (a), D8 (as proposed), D9 (a), D11 (mover retired) and D15 (v3.0.0). AR0-M lists what stays pending; **M0 has the go-ahead and AL-S8's baseline is measured** (`6ead2a0`, three files under `bench/v3.0.0/`) |
| `workorder-al-m0-single-wal.md` | **AR0 M0 — the single WAL stream.** The survey of every per-core-stream assumption at `d15b5ac` (AL-3), the rulings M0 needs (AL-4, with AL-R1 the append mechanism as the one that decides the shape), and the stages AL-S0..S9 with their cells and sizes (AL-5). AL-S0 is the hour the operator ordered |
| `ratification-an-commit-order.md` | **A global commit sequence is admissible.** Rules that `cross-owner-txn.md` §1 rejected a global counter and **never justified it** — its stated reason reaches the shared transaction id beside it, and only on a per-core volume — so §3's cross-reference inherits an empty justification. AN-D4 makes the commit record's LSN the ordering authority under `kSingleStream`, invariant 12 untouched. **Marked 2026-09-03** (AN-D10): AN-Q1, Q2 and Q4 are settled by the operator's mark of `workorder-an-read-view.md` AN-R7(B) and built there; Q3, Q5 and Q6 moved into that work order as AN-R9, AN-R10 and AN-R11 — **not** into M2's table, as this document originally wrote its own exit. AN-D10 also carries the correction AN-R8 makes to AN-Q1's floor. **A prerequisite to D1's condition, not a milestone**: no code, six sites across four specs. AN-7 records the four claims a `critics-developer` review refuted in the first draft |
| `workorder-am-m1-shared-pool.md` | **AR0 M1 — the shared buffer pool, page latches, scalar page LSN.** Written 2026-09-03 while M0's baseline was still measuring. **AM-S0 (a) and (b) are done** (`f710b3d`, `44401f9`: the assembly under test at `cores = 2`, the fixture's per-core-stream override deleted) and **AM-S1 is built at `c985d37`** — the page latch word, armed only at `cores > 1`, **outer** to the WAL stream latch — with its `cores = 1` A/B at `680c763` (`bench/v3.0.0/results-am-s1-page-latch-v2.7.0-183-gc985d37.md`: the compile-out's cost bounded at −1.9% group and +2.4% strict under pair-wise exclusion, corrected 2026-09-04 from a cell-wise reading that found nothing measurable; the armed cost unmeasured until AM-S6); AM-S2..S6 are not started, and AM-S2 inherits the page-against-page order AM-S1 left unstated (AM-6 is the row table). Two survey findings reshape the milestone — "scalar page LSN" is already done, and "page latches" is a first build rather than a widening, since `base/latch.hpp` has exactly one includer in the tree |
| `ar1-architecture-revision-cabin-function.md` | **A second governing draft, and it is not AR0's successor** — scalar functions, a purity class per function, a determinism class per statement, and a Cabin generalised from `(relation, column)` to a key function `F`, with C3 struck and the chain shape making a join's result set a witnessed Cabin. **DRAFT, pending ratification**, and independent of the pool and the view but for §11's one crossing. **AR1-V appended**: the source read at `74f971b`, which is what the tree says wherever the body disagrees. It disagrees twice that matters — §3's quiet-wrong hazard is refuted (rule 0 is structural over `(step_id, pk)` for the *driving* step too, so D8 is a wording change and costs nothing), and with it §14's reason for sequencing AP first; §7's migration bit does not exist |
| `ar2-architecture-revision-borrow-model.md` | **A third governing draft, a refinement of AR0-4 inside M2 and M3** — write ownership replaced by *borrowing*: a scope-bounded tenancy over one of five units (relation, range, page, slice, tuple) in two families (the page latch outside the hierarchy, intention-moded locks on the other four), with a normative unit table per operation (§3) that AR0's "row locks + page latches" does not have. Three rules carry the `[quiet-wrong]` tag — scope-bounded never time-bounded, a slice is keyed in key space, the FK child side borrows what can be named — beside a fourth headline rule tagged `[design]`: an `INSERT` borrows a range's id block rather than a page. **DRAFT, partly ratified by `raft-ar2-A.md`**, nothing before M1, nothing touching M1; E1–E13 are its operator items — E12 the one cost it puts on reads, an `IS` at the position's finest lock unit so a move can wait for a positioned reader. **C1 and C2 measured at `92cb654`** (`bench/v3.0.0/results-ar2-c1-colocation-…`, `…-c2-spreading-…`): a hop-free route recovers the whole 46% AL-S8 found, and local inserts under spreading lose 24–26% with the loss landing on the unchanged shipped `UPDATE`; they price E7 and gate nothing. **AR2-V appended**: the source read at `183b956`, and one discrepancy found on the way — `physical-optimizer.md` §6's first gate cites a reader-registration premise `txn.md` §4.1 no longer holds (corrected 2026-09-04 in that spec and `relayout_planner.hpp`); AR2's §2 table, §8 R6 line and §9 C2 class were corrected the same day for the drifts AO-7 and this branch's review named |
| `raft-ar2-A.md` | **Ratification AR2-A** (operator, 2026-09-03): AR2 is evaluated on refusal → wait, flexibility and the physical optimizer's foundation, not on throughput; the mover borrows the unit whose key-space assignment it changes (R13) and the read borrow sits at the position's finest lock unit (R14); E1, E4, E11, E12 and R4's refusal ratified, the rest deferred with gates; six amendments to the AR2 body, applied on the branch at `988a698` and marked (AR2-A). M2 opens when M1 and AN-S2 close |
| `workorder-ao-m2-lock-family.md` | **AR0 M2 — the lock family**, the stated M2 AR2-A §6 asked for so AM and AN proceed against it. Written 2026-09-03 at `9e5068c`: the refusal census (13 rows, each classed by locality and each turned into a wait, retired, or kept), the survey of the wait substrate (`WaitUntil` is a poll loop; a same-core grant needs no wake; the in-doubt block is a working prototype of a tuple-lock wait that ends by clock), rulings AO-R1..R14 — among them **no persisted lock bit** (the `trx_id` stamp is the tuple `X` fast path; the Keystone byte stays zero, decided by the operator in plan mode) and D12's detector as the mechanism, with a fault net that aborts the waiter — `kTxnPhaseDeadlineNs + 1 s` while 2PC is in the tree, 1 s once M3 retires it (AO-R8) — and stages AO-S0..S8 with S1–S4a and S3b gated on the operator's word alone and S5 onward on AM-S1..S6 and AN-S2. **AO-S0 landed with this row**: the document and `rules.md` §3's lock-table row, marked not built. **AO-S1 built 2026-09-04** on the operator's word — `include/kds/txn/lock_table.hpp`, the lock family's pure core with no scheduler in it: four units, four modes as `constexpr` tables, `64 x cores` partitions latchless at one core, AO-R3's fence counter in place of the declined Keystone lock bit, and the cap as the one refusal the model adds. **AO-S2 built the same day**: a refused borrow hands back a slot, the waiter parks on it and re-asks on every wake, a decide wakes who was queued, and `Transaction` carries its borrows for the manager to release last. The two-session-on-one-reactor fixture the order says does not exist now does. No core constructs a table until AO-S3. **AO-S3 built the same day** and is the cutover: a write now waits for any undecided holder rather than refusing, the foreign-key forward check waits for its parent, and the 200 ms ceiling becomes an 11 s fault net that is logged as a fault. Its review found that widening alone opens a two-transaction deadlock with no detector to end it, so a wait is offered only to a transaction that holds nothing — which makes the stage deadlock-free by construction. **AO-S4a built the same day** lifts that guard where a detector exists: a `waiter -> holder` edge is recorded before every park and a waiter that would close a cycle is refused naming deadlock, detected at registration rather than on AO-R7's cadence because that is when a cycle can close. Wired at one core only, where instance-wide and per-core are the same object. **AO-S5(a) built 2026-09-07** (the rig, the wake as write-then-kick, and the table across reactors - one `LockTable` built by the `Expeditor`, borrowed by every peer, kicking through the wake registry; the finding that no server had a lock table before it), **AO-S4b built the same day**: the shipped-statement park records its edge on the owner (the request carries the coordinator's transaction id; the ceiling falls 968 → 960 bytes - 976 → 968 on its own branch, AN-S3's eight having landed in the same merge), every dispatcher takes the instance table, and a two-core cycle through shipped statements is refused at the registration that closes it, the refusal riding the reply - no cadence, no `kLockAbort`. **AO-S5(b) built the same day** once AN-S2 and AN-S3 were on `main`: the forward probe parks on the parent's core until the writer decides (the child's id on the wire, the edge on the child's behalf, the re-check from a fresh view), so a cross-core foreign-key check waits where the holder is instead of refusing `busy`; the reverse probe's busy and the `MayWrite` cell's absence are stated. Its review left two edges open, **closed 2026-09-08 on `ao-s5b-c1c2` as C1 and C2**: the probe's check view carries the requester's own participant on the parent's core as its writer, so a transaction writing its own parent is answered from its own pending image rather than parked on it; and a decide that reaches a parked probe abandons the park, so nothing is granted after the child has decided. Underneath C2 a pre-existing defect: `Txn2pcClient::OpenPhase` refused the transaction-0 decide that `DispatchAsync`'s refusal arm and `ReleaseIntentsWithoutWaiting` send by design, so an autocommit statement refused after its probes had left never released the intents it was granted - the zero check now lives on `Prepare`, and on `Decide` only over a target that prepared. C9 open. AO-S6..S8 not started |
| `workorder-an-read-view.md` | **The instance read view**, ordered by the operator on 2026-09-03 to be built *now*, between AM-S0 and AM-S1. AN-1 is the argument that a per-core view plus a shared pool is a dirty read rather than an error, and that whichever milestone first lets a core read another core's data page locally must carry this one. The AN-S0 source read found that trx ids are leased in per-core blocks, so **no bound on trx ids can order commits across cores** (AN-3 E's H1 and H2), and the operator marked AN-R7 **(B)**: the view is the commit-LSN window of `ratification-an-commit-order.md`, and the in-flight set retires rather than being shared. **AN-S0 and AN-S1 landed at `b5abab6` with the suite green; AN-S2 is gated on AN-R10** — the retention policy the mark was taken without. AN-7 records both review passes: CLA's source read (four findings, seven corrections) and a `critics-developer` pass that refuted AN-R1's lock-free justification for a non-monotone field and AN-R9's durability ground, neither of which survives as written |
| `ar0-5-amendment-uniformity.md` | **AR0-5, an amendment to AR0 §2** and the fifth item of its §1 decision series: on the operator's word of 2026-09-05, *no page and no relation has an owner and core 0 is not a role*. Where AR0 retired one of three ownership properties, AR0-5 retires all three — allocator authority included — and puts three synchronisation primitives in their place: a schema version word (fast path only; the relation `IS`/`X` of AR2 R14 is the argument, and §8 states the one quiet-wrong surface if the two are read in the other order), shared allocators with authority-free per-core caches, and the writer thread AL-S1a/S1b already built. §4 is the retire list by milestone — M1 takes `MayFault`, M2 takes `MayWrite`'s **grant arm only** (AM-R2 and AO-R14 keep the rest while owner routing is in force, and an earlier draft retired it at M2, which would have left a lost update with no gate), and everything else goes at **M3, renamed "Uniformity", provisional letter AT** whose work order is not yet written. Six new operator items, D17–D22. **DRAFT, pending ratification. AR0-5-V appended**, then corrected by a `critics-developer` pass that found the verification section the least reliable part of the document: seven `path:line` citations had drifted (counted as six), the cause given for two of them was false — the +19 on `device_page_store.cpp` is AM-S1's page latch, not any AO stage, which shows the draft was written against a pre-`c985d37` tree — and one "correction" pointed at prose in a comment. The `(M5)` census found five of **twelve** sites. What survived intact is the reasoning: `crosscore.md` has no `M5`, the rule is CC11, and the bare `M5` in those comments is the pre-compaction milestone rather than AR0-M5 |
| `workorder-as-sus1-heap-suspended.md` | **SUS-1 — heap relations suspended** (operator, 2026-09-05), and **not part of AR0's milestone chain**: AS depends on no work order and gates none, which is why it lands between AO stages. No new heap relation is created — `CREATE TABLE … HEAP` is refused `Unsupported` naming SUS-1, the storage default flips to `BTREE` — while **everything below the parser is untouched**: an existing heap relation mounts, serves, walks its chain and replays. Nothing is deleted; heap-only cells take a `heap-suspended` ctest label and a test-only bypass keeps their paths executing. Three survey facts shape it: the default is spelled **twice** (`ast.hpp:415` and `command_dispatcher.cpp:5431`, the second being the one that runs on the dispatch path), **~525 of 759** test relations ride that default and are what could make the S sizing wrong, and the order's resume condition cites `parser-v2` §8, which has been empty since the compaction — the three-way corpus is **§9 item 8**. **AS-S0 written; AS-S1 (the code) not started.** Four operator items, AS-Q1–Q4. **AS-Q6 marked 2026-09-08** (`raft-marks-2026-09-08-as-q6.md`): the scenario drivers go BTREE, `f6ed10c` is re-measured with them as the new baseline, and measurement is BTREE only for now |
| `raft-marks-2026-09-08-as-q6.md` | **The operator's mark of 2026-09-08 on AS-Q6**, in Korean: change the drivers, re-measure `f6ed10c` with the same drivers as a new baseline, measure BTREE only for now. What it obliges, in four rows - the two scenario drivers' four heap relations become `BTREE` with the PostgreSQL twins mirroring it and no storage switch added; `f6ed10c`'s archived binary re-measured with the changed drivers into fresh `bench/v3.0.0/` files that are the comparator from then on; the BTREE-only rule in `bench/README.md`; AM-S6 and AN-S5's scenario half unblocked as deltas against that baseline - and what it does not cover: the **five** other tools that still emit an explicit `HEAP` and are refused — among them `tools/scenario1_backtest.py`, a third scenario driver of the same family this mark leaves untouched — plus `cabin_scope_ab_benchmark.py`, which emits no storage word and so silently measures a btree it labels heap. All AS-S3's |
| `workorder-am-s2-p-scan-ring-port.md` | **AM-S2 step 3's scan ring, decided twice and merged once.** Two sessions built step 3 in parallel from `ce4d27d`: `main` landed the whole of it (3a..3g, `2663001`, `dd0bfe9`) with a ring that pins **every occupied slot**, and `worktree-am-s2` landed one commit, `c7c3a67`, with a ring that pins **one page and latches it**. On the operator's word of 2026-09-06 - combine, better code per item - this order is the per-item verdict and the port. **AM-R8 final**: one shared, page-latched pin on the page the last `Fetch` returned, the `loading_` protocol instead of `PinForScan`'s eight-attempt `ResourceExhausted`, and a slot rotated only on a fault. The latch is the correctness half - without it a scan reads a page a writer on another core is mid-write on - and §1.3 retires the argument that a latch there would deadlock, which `UnpinFrame` and `PlainScanFetcher` had already refuted. **S-P0's read moved four of this order's own rulings**: `main` had closed R3 (by pinning where the load finished, so the second fetch never runs) and the 2a/2b R6 (by moving MG06's sweep inside `InsertFrame`'s hold, and a budgeted cell) better than the branch had, so `charged`, `inline_sweeps`, `frame_usage_for_test` and the eraser latching are superseded rather than merged. **And F-1 was withdrawn**: S-P0 filed a lost write against `main`'s R3 fix, CLA built the guard, then found the `loading_` set already closes it and the one path that inserted without publishing was the ring's - so AM-R8a *is* the fix and the hazard had no reachable case - the guard's own cell would have passed with the guard reverted, which is the third time that pattern surfaced in this milestone. S-P0..S-P3 landed `75f81a8`, `6cbd6f8`, `7804a33`, `a140e8d`; seven mutants, seven kills, one cell written only because a mutation survived first (`always-rotate` passes every other ring cell), and seven of CLA's own comment claims found false by the verification passes. Supersedes `workorder-am-s2-step3-scan-ring.md`, which lived on `worktree-am-s2` and is retrievable at `c7c3a67`, and `workorder-am-s2-merge.md`, which was never written to any tree |
| `raft-marks-2026-09-08.md` | **The operator's marks of 2026-09-08 on four AO-0 rows**, verbal and recorded verbatim: the borrow cap at 65,536 as `kds.max_locks_per_txn`, `[provisional]`, refused `ResourceExhausted` non-retryable and never escalated (§1); the subsystem named `lock`, with `borrow` the tenancy and `latch` the critical-section family (§2); waits at `cores = 1` confirmed as shipped behaviour (§3); the partition latch a mutex first, amending AR0 D2(a), with the spin's switch condition to be written into AO-S7's order before it runs (§4). §5 lists what the marks do not reach, and §6 where they land. Two of §5's rows were answered later the same day and are recorded where those answers land: `raft-marks-2026-09-08-as-q6.md` and `workorder-ax-inflight-publication.md` |
| `raft-marks-2026-09-05.md` | **The operator's marks of 2026-09-05**, verbal: *"the pending decision list follows CLA's proposals; write the work order."* Four blocks — AM-S2 step 3's four items, AR0-5's D17–D22, AR0-6's D24–D26, AS's Q1–Q6 — each row naming what the mark obliges rather than restating the proposal. Its §5 is the half that matters most: **a blanket "follow CLA" adopts nothing where CLA filed no proposal**, so `workorder-an-read-view.md`'s AN-R10 stays open and AN-S2 stays gated, with the three candidate shapes written down so the operator's next word can be a choice rather than a request for options. **Written on `worktree-am-s2` and carried onto `main` only at AM-S2-P S-P4**, which is why §1 reads as history: a second session decided AM-S2's ring the other way the same day and landed first, so §1's rows say where each mark ended up rather than what it obliged. §2–§5 were untouched by that and exist nowhere else in the tree, which is why the document had to cross before `worktree-am-s2` was retired |
| `ar0-6-amendment-ring-retirement.md` | **AR0-6, an amendment to AR0 §3** and the sixth item of its §1 decision series (CLA, 2026-09-05, at `4bd73d7`): the ring transport is retired and **the wake is the one cross-core primitive** - write the shared state under its own latch, then kick the destination's reactor; the kick carries no meaning (AR0-6-R1). Its §1a is the finding that shapes the retirement: of the transport's **35** kinds (the draft said twelve; AR0-6-V corrected it), eighteen are request/reply protocols carrying real operands that die with *ownership* at AT, not with the transport, so the ring outlives the amendment by construction and AU's job is to build the wake and stop the ring acquiring consumers. D23–D26 are its operator items; D26 rules the rig order's letter is AV. **DRAFT, pending ratification; AR0-6-V appended** |
| `workorder-au-ring-retirement.md` | **AU — ring retirement: the wake first, the transport last**, under AR0-6. AU-R1 makes `Kick(core)` the operation and `Wake()` its implementation; AU-R2 makes every cross-core wake write-then-kick; AU-R4 freezes the kind enum at its true size with a `static_assert`, so a kind is only ever struck. **AU-S1 built** (`WakerTable`, the instance's map from core id to the destination's `sleeping` flag and `Waker`; the survey found `may_sleep` gated on the transport, so retiring it would have silently disabled every cross-core wake), **AU-S1b built** (the two registries collapsed into one: a send and a stop kick through the same table), **AU-S3 built** (`kShutdown` struck for an atomic stop flag plus a kick; count 35 → 34). **AU-S1 reported itself built while dropping `SimWaker`**, carried as AU-S1c - and AU-R3 could not be built as written, `Waker::Wake()` being non-virtual (`workorder-av-two-core-rig.md` AV-R1); on the operator's mark of 2026-09-07 the seam is the **table**, AU-R3 is rewritten to `SimWakerTable`, and **AU-S1c is built the same day**: `WakeRegistry` at the two attach points, `WakerTable` its one production implementation, `SimWakerTable` wrapping a real one - `Kick` logs `(tick, dst)` and forwards at `tick + delay(seed)`, the tick a counter the rig advances. **AU-S2 built the same day, on the rig**: a waiter records the core it parks on and a decide's slot flip kicks that core through the registry after the partition latch drops - no `kLockWake`; its cells are `workorder-av-two-core-rig.md` §6's 4, 6 and 7 at the table's level, and its review found the instance table putting a mutex on every commit, gated now. S4 rides AM-S2 and AO-S5; S5 and S6 are inside AT |
| `workorder-aw-m1-close.md` | **AW — M1 close and the read-view cutover**, and a **sequencing order**: it opens no milestone, takes the stages it names in the two that are open (M1's AM-S5/S6 and AN-S2 — **AM-S4's deletion half was open too**, the lease, `MayFault`, the CC7 fault grants, `TryClaimByStamp` and `CoreRuntimePerCoreStreamTest` still in the tree at `15a57c2` against AM-R4a's "together or not at all", and this order carried no stage for it as first written — the operator inserted **AW-S1b** for it, and it landed 2026-09-07: all of AM-S4 but the stamp field and the per-core-stream mount branches, 44 cells retired with the machinery, sized M and delivered L), and writes the one document that gates what comes after them. The critical path it clears is `AM-S6 → AN-S2 → AO-S5 ← AU-S2 ← AV`, every gate but AU-S2 itself. **Three operator decisions of 2026-09-06** carry it: AN-R10 and AN-R12 marked as `workorder-an-read-view.md` proposes them; the interleaved A/B suspension **lifted for AM-S6 alone**, on AL-S8's own host, because D15 permits a delta only within one engine on one host and that delta is the stage's whole deliverable — `CLAUDE.md:221` stands everywhere else; and **transaction lifetime**, the operator's numbers: a 10 s envelope and a 60 s ceiling, recorded as **AN-R14**. That ruling re-scopes the *ceiling* — `kShippedTxnIdleCeilingNs`'s 300 s of *idleness* is unreachable under a 60 s *lifetime* and is absorbed into `kds.txn_lifetime_ceiling`, one quantity with one name — while the mechanism under it is new: no transaction carries a wall-clock start today, so a lifetime ceiling is a field plus a sweep, and the constant has 20 references across 8 files where the order's own list names 3 — and it states its own cost: a long **busy** transaction is now aborted where CLA's first proposal accepted it, with the cell CLA's proposal did not have. Three exemptions, each with its reason so nobody later strikes them as special cases: DDL, background read views (which re-mint and resume rather than abort), and prepared contexts. **AW-S0, S1, S1b, S2 and S6 landed; S3 half; S4 surveyed; S5 gated on AM-S6's host** |
| `workorder-av-two-core-rig.md` | **AV — the deterministic two-core rig**, drafted at AW-S6 and **built through AV-S1 on 2026-09-07** (its §8 is the AV-S0 read of every `Expeditor` wiring step, its §9 the rows): two `CoreRuntime`s over one store, one stream, one visibility and one waker table, with `WakerTable::Kick` the only cross-core primitive, both reactors on `CoreRuntime::Run()` and the kicks logged and held by a `SimWakerTable` (`tests/two_core_rig.hpp`). Core 0 is a `CoreRuntime` there rather than an `Expeditor`, which cost two runtime fields and no seam in `Expeditor` (H4). It is the last ungated item on the critical path — AU-S2's gate is "the rig, on `SimWakerTable`" and AO-S5's first deliverable *is* the rig — which is why AW marks it parallel. **Two citation corrections it had to make before it could start**: AW-S6 asks it to cite "AU-R6", which does not exist (the AU order stops at AU-R5; the ruling meant is **AU-R3**), and `SimWaker` is owed by the unbuilt **AU-S1c**, not by a ruling. **And one finding that blocks the rig**: AU-S1c says `SimWaker` substitutes through `WakerTable`'s `const Waker*` so "nothing in the engine learns a new type" — but `Waker::Wake()` is **not virtual** (`waker.hpp:66`) and `Kick` calls it statically, so that cannot compile. AV-R1 sets out four seams and proposes substituting the *table* rather than the waker; whichever is taken rewrites AU-R3's sentence. §6 lists all eight cells the rig must host by name, including the AM-S2-P cell whose test hook it retires, and flags that AO-S5's cell 8 lost its premise when AW-a retired `MayWrite`'s grant arm |
| `workorder-ax-inflight-publication.md` | **AX — the instance in-flight publication**, opened 2026-09-08 on the operator's acceptance of a proposal the tree does not carry (the order says so and takes the operator's sentence as the contract): per core, one slot, written only by the issuing core, read atomically by every core, the active bit and nothing else — `InstanceVisibility`'s shape applied to a set. The survey finds one consumer wrong across cores today: DT9's delete-mark gate in `catalog.cpp`'s `ScanAll` asks a peer's own manager about core 0's DDL transaction and counts an uncommitted drop as settled. Six rulings (a dense single-writer table per core, presence as the active bit, the query instance-wide, a cap with refusal as the only honest overflow, the consumers, `cores = 1` unchanged), three stages, two operator items (the cap's value; the DT9 behaviour change on peers). **AX-S0 written; AX-S1 waits for AM-S6's measurement to leave the host** |

**Read AR0-V before citing AR0's body.** Three of the body's numbers are
already attributed in the tree to causes other than the ones it names,
and two of its D-items name values the tree never shipped.

## What is *not* here

Two directories reopened on 2026-09-03 and each took a job this one used
to cover: **`bench/`**, where a v3 measurement goes (AL-R8; its README has
the rules), and **`docs/inflight/`**, which holds what is *missing*
(its README has the buckets). Open work orders stay here.

The one boundary worth stating twice, because getting it wrong is how a
list goes stale: a **decision** awaiting the operator lives in its work
order's ruling table — AR0's D1-D16 are indexed there, per milestone — and
a **gap** lives in `docs/inflight/known-gaps.md`. Never both.

**One form sits beside that boundary rather than inside it**, added
2026-09-03 with AN: a ruling whose subject is a **spec sentence** and whose
milestone has no work order yet gets its own `ratification-*.md` here. It
is not a third bucket — it is a holding shape with an exit written into it:
when the milestone's work order is opened, the ruling's `[OPEN]` items move
into that table and the document keeps only what it settled. A ruling that
would fit an existing work order's table does **not** take this form.

**That exit was taken on 2026-09-03, against a different milestone than the
one it named.** `ratification-an-commit-order.md` wrote its exit as *"when
M2's work order exists"*, on the reading that a commit sequence is
lock-manager work. `workorder-an-read-view.md` opened first and its subject
is the same object, so the operator's mark of AN-R7(B) is what made it that
work order: AN-Q1, Q2 and Q4 are settled by the mark and built there, and
AN-Q3, Q5 and Q6 moved into its ruling table as AN-R9, AN-R10 and AN-R11.
`ratification-an-commit-order.md` keeps AN-D1..D4, D6, D7 and D9, and its
AN-D10 records the move and one correction the mark inherits. **Two
documents still carry the letter AN**, which is why `CLAUDE.md`'s rule is
the whole protection here: **cite the file, never the bare number.**
