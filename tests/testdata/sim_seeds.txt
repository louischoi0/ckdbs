# The committed simulation seed corpus (bench/workplan-teststrategy SIM04).
# Every seed here is run by tests/sim_loop_test.cpp on every CI run,
# forever. Append a seed when a run of ckdbs-sim finds a failure (after the
# engine fix lands, the seed stays as the regression); removing one takes
# the same justification as deleting a test.
#
# Format: one decimal seed per line; '#' comments.
1
2
3
4
5
# H9: an append-split whose separator promotion met an injected page-fail-grow
# left the new leaf linked into the sibling chain and routed by nothing, so a
# later append spliced its leaf in front of it and the chain descended
# (`min_key 78 does not exceed a predecessor page's max id 183`). Fixed by
# promoting the separator before the sibling link; btree.cpp carries the
# ordering argument.
20260826003
